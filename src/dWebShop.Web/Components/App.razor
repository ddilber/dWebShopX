<!DOCTYPE html>
<html lang="bs">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <meta name="description" content="ASGifiks — ovlašteni distributer STO, XYPEX i CORTEC sistema za BiH. Fasadni sistemi, kristalna hidroizolacija betona i antikorozivna zaštita. Tomislavgrad, od 1998." />
    <meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1" />
    <meta name="theme-color" content="#00335E" />
    <meta property="og:site_name" content="A&amp;S GIFIKS" />
    <meta property="og:locale" content="bs_BA" />
    <meta name="twitter:card" content="summary_large_image" />
    @* og:type and og:image are set per-page (see Seo.OgImageTags) so social
       crawlers get exactly one, page-specific image. Pages that omit og:type
       are treated as "website" by default. *@
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="@Assets["css/app.css"]" />
    <link rel="stylesheet" href="@Assets["css/landing.css"]" />
    <link rel="stylesheet" href="@Assets["css/brand-pages.css"]" />
    <link rel="stylesheet" href="@Assets["css/inspiration.css"]" />
    <link rel="stylesheet" href="@Assets["dWebShop.Web.styles.css"]" />
    <ImportMap />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
    <script>
        // Disable browser scroll restoration as early as possible (before any layout/paint).
        if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
    </script>
</head>

<body>
    <Routes />
    <ReconnectModal />
    <script src="@Assets["_framework/blazor.web.js"]"></script>
    <!-- DEV-ONLY: live font preview panel. Remove this line + wwwroot/js/font-switcher.js to disable. -->
    @* <script src="@Assets["js/font-switcher.js"]"></script> *@
    <script>
        window.scrollToTop = function() { window.scrollTo({ top: 0, behavior: "instant" }); };

        // Scroll to top on navigation and reload. We cover three navigation paths:
        //  1. Initial page load / F5  — direct scrollTo (browser restoration already disabled in <head>).
        //  2. Enhanced SSR navigation — fires `enhancedload`.
        //  3. InteractiveServer / NavigationManager — calls history.pushState (monkey-patched).
        // popstate (back/forward) intentionally not handled here; with manual restoration the page
        // simply starts at top, which the user explicitly wants.
        (function() {
            function toTop() { window.scrollTo({ top: 0, behavior: 'instant' }); }
            toTop();
            window.addEventListener('load', toTop);

            document.addEventListener('enhancedload', toTop);

            var origPush = history.pushState;
            history.pushState = function() {
                var ret = origPush.apply(this, arguments);
                toTop();
                return ret;
            };

            window.addEventListener('popstate', toTop);
        })();

        // Scroll-triggered animations for .animate-on-scroll elements
        (function() {
            function initScrollAnimations() {
                var elements = document.querySelectorAll('.animate-on-scroll');
                if (!elements.length) return;
                if (!('IntersectionObserver' in window)) {
                    elements.forEach(function(el) { el.classList.add('animated'); });
                    return;
                }
                var observer = new IntersectionObserver(function(entries) {
                    entries.forEach(function(entry) {
                        if (entry.isIntersecting) {
                            entry.target.classList.add('animated');
                            observer.unobserve(entry.target);
                        }
                    });
                }, { threshold: 0.1 });
                elements.forEach(function(el) { observer.observe(el); });
            }
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', initScrollAnimations);
            } else {
                initScrollAnimations();
            }
            // Re-run after Blazor enhanced navigation
            document.addEventListener('enhancedload', initScrollAnimations);
        })();
    </script>
</body>

</html>

